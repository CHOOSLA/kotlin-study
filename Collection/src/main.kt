fun main(){
    // List : 모여잇는 데이터가 순서가 있고 중복 허용
    val list = listOf("문자열입니다.",3,true)
    println("list: $list")

    // Set : 모여있는 데이터의 순서가 없음, 중복 허용 안 함 -> 집합 느낌
    val set = setOf("문자열입니다", 3, true, 3)
    println("set: $set")

    // Map: 키-값 쌍을 이루는 데이터의 모음

}